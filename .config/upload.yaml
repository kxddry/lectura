env: local
s3storage:
  endpoint: minio:9000
  access_key: ROOTUSER
  secret: CHANGEME123
  ssl: false

server:
  address: 0.0.0.0:8000
  timeout: 90s
  idle_timeout: 120s

writer:
  brokers: [kafka:9092]
  topic: file.uploaded
  client_id: uploader

clients:
  sso:
    address: auth:42042
    timeout: 5s
    retries: 5
    insecure: true

pubkey_path: /public.pem
privkey_path: /private.pem

public_keys: []